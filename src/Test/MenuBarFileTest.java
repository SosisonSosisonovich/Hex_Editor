package Test;

import java.io.FileOutputStream;

public class MenuBarFileTest {
   /* try (FileOutputStream fos = new FileOutputStream(testFile)) {
        // Устанавливаем размер файла в 1 гигабайт
        //fos.write(new byte[1073741824]);
    }*/
}
